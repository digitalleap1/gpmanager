# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database / ORM / migrations ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth / security ---
python-jose[cryptography]==3.3.0
bcrypt==4.0.1            # used directly for password hashing (no passlib)
python-multipart==0.0.20

# --- Bulk import/export (Excel) ---
openpyxl==3.1.5

# --- Misc ---
python-dotenv==1.0.1
