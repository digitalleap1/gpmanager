# Development & test tooling (install on top of requirements.txt)
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1            # FastAPI TestClient transport
ruff==0.8.4             # linter + formatter
mypy==1.14.0
