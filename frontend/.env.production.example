# Frontend production env (set these in the Vercel dashboard → Project → Settings → Environment Variables).
# Point this at your deployed backend's /api base.
NEXT_PUBLIC_API_URL=https://gpoms-api.onrender.com/api
